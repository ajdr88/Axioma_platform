# infrastructure

**Not started.** Provider-parameterized Terraform/K8s, arriving with the deployment-mode
readiness work (NFR-COMP-01…05 in `docs/Axioma_requirements_v3.md` §3.4): cloud portability,
per-project region pinning, auth abstraction, audit logging, single-tenant isolation. Not required
for local development — see the root `docker-compose.yml` and `.devcontainer/` for that.
