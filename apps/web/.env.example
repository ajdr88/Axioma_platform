# Copy to .env.local for local development. Read server-side only, by the Route Handler proxies
# in src/app/api/*/route.ts — never exposed to the browser, so no NEXT_PUBLIC_ prefix.
API_URL=http://localhost:8080
