# llm-gateway

**Not started.** Pluggable LLM provider interface (local Ollama default, hosted optional) per
ADR-004 in `docs/Axioma_implementation_v3.md` §2.5. Backs Mode A (copilot) and AI-assisted
drafting only — `cem-core` decisions stay deterministic and never call this package.
