# Regenerated by fetch-deps.sh/build.sh — vendored jars and downloaded toolchain binaries don't
# belong in git (see fetch-deps.sh's own comment); `out/` (compiled classes) is covered by the
# root .gitignore already.
lib/
tools/
generated/
