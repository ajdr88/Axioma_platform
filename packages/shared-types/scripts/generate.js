// Placeholder for Rust-struct -> TS type codegen (impl §3.1). Not implemented yet — until it is,
// src/index.ts is hand-written and must be kept in sync with packages/sysml-core/src/lib.rs
// manually. A real implementation would likely run `cargo run --bin export-schema` (or a
// ts-rs / typeshare-style derive in sysml-core) and write the result to src/index.ts.
console.log(
  "shared-types codegen is not implemented yet — src/index.ts is hand-written, see packages/sysml-core/src/lib.rs",
);
